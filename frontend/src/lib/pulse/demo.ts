/**
 * Company opened by the "PULSE" entry of the navigation.
 *
 * Eight months observed, 82 % of the score backed by data and a forecast that
 * stays below 35: the case the demo tells, where the confidence figure matters
 * as much as the score itself.
 */
export const PULSE_DEMO_COMPANY_ID = 'COMP_0001';

/**
 * Company used to show the two credit-line variables in use.
 *
 * It has 24 observed months and a known `loc_util`, so utilisation and its
 * acceleration are scored instead of reported as «sin datos».
 */
export const PULSE_DEMO_CREDIT_LINE_COMPANY_ID = 'COMP_0051';
