# Environment variables loaded with python-dotenv. Copy to .env and fill in.

# --- API (src/ml_service/api) ---
# XRAY_DATA_DIR=./data
# XRAY_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# XRAY_API_KEY=                # optional; guards POST /api/score*
# PORT=8000
