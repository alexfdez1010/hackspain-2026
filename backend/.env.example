# Environment variables loaded with python-dotenv. Copy to .env and fill in.

# --- PULSE engine (src/ml_service/pulse) ---
# PULSE_DATA_DIR=./data          # raw CSVs in <dir>/raw/xray, artefacts in <dir>/pulse

# --- API (src/ml_service/api) ---
# PULSE_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# PORT=8000
