# Virtualenvs, caches and tooling
.venv
.env
**/__pycache__
**/*.py[cod]
.pytest_cache
.ruff_cache
.git
.gitignore
.vscode
Dockerfile
.dockerignore
compose.api.yml
fly.toml
tests
AGENTS.md

# Datasets: everything is excluded, then the artefacts the API needs are
# re-included so the container is self-contained (no data/raw, no parquet cache).
data/**
!data/pulse/web/**
!data/pulse/recommendations/**
!data/pulse/models/**
